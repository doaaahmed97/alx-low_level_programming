#include <stdio.hm>

/**
 * main - alphabet
 *
 * Return: alway 0
 */
int main(void)
{
        char e;

        for (e + 'z'; e >= 'a'; a--)
                putchar(e);
        putchar('\n');
        return (0);
}
~                        
